export enum EPaymentType {
  UNIQUE = "UNIQUE",
  INSTALLMENT = "INSTALLMENT",
  RECURRENT = "RECURRENT",
}

export enum EFlow {
  IN = "IN",
  OUT = "OUT",
}

export enum EStatus {
  PENDING = "PENDING",
  PAID = "PAID",
  CANCELLED = "CANCELLED",
  LATE = "LATE",
  PAYDAY = "PAYDAY",
}
