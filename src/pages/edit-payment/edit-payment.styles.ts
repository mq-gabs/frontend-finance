import styled from "styled-components";
import text from "../../assets/theme/text";

export const StyledEditPayment = styled.main`
  h2 {
    font-size: ${text.fontSize.xl};
  }
`;

export const StyledForm = styled.form`
  display: grid;
  gap: 1rem;
  max-width: 500px;
  margin: 0 auto;
`;
